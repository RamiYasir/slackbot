# Timesheet slackbot 

## Architecture

This WIP includes an API gateway with access to a lambda, which currently returns dummy data but which will eventually interact with a Dynamo database. 

Eventually I'll create a diagram that's worth adding to this README.